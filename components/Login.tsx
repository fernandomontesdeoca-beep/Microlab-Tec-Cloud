
import React from 'react';

// This component is no longer used as Firebase Auth has been removed.
// Kept as a placeholder to prevent import errors if referenced elsewhere.

export const Login = ({ onLogin, onBypass }: any) => {
    return <div className="p-10 text-center text-gray-500">Login Disabled (IndexedDB Mode)</div>;
};
